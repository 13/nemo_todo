# Dart / Flutter
.dart_tool/
build/
coverage/
*.log
.flutter-plugins
.flutter-plugins-dependencies
.packages

# IDE
.idea/
*.iml
.vscode/

# Android
app/android/.gradle/
app/android/key.properties
app/android/local.properties
**/android/**/gradle-wrapper.jar
**/android/app/.cxx/

# Web assets fetched by tool/fetch_web_assets.sh
app/web/sqlite3.wasm
app/web/drift_worker.js

# Runtime
.env
# Anchored: an unanchored `data/` also swallowed every feature's data
# directory, and those source files silently never reached the repository.
/data/

# Knowledge graph built from this repo by graphify. Regenerated on demand,
# and large enough that a diff of it says nothing useful.
/graphify-out/

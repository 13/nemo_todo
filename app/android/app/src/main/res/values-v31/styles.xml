<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12 draws the splash itself and ignores windowBackground, so
         the brand colour and the mark are handed to the splash screen API.
         Without this the system falls back to the launcher icon on the
         window background, which put a white mark on white. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
    </style>
</resources>

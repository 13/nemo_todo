import 'package:flutter/material.dart';

/// Placeholder until the real screen lands.
class  extends StatelessWidget {
  const ({required this., super.key});

  final String ;

  @override
  Widget build(BuildContext context) =>
      Scaffold(body: Center(child: Text(' $')));
}

name: nemo_server
description: nemo sync server. Serves the API and the built web app.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  args: ^2.6.0
  bcrypt: ^1.2.0
  crypto: ^3.0.6
  drift: ^2.34.4
  logging: ^1.2.0
  meta: ^1.16.0
  nemo_core:
    path: ../packages/nemo_core
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_static: ^1.1.3
  sqlite3: ^3.5.2
  uuid: ^4.6.0

dev_dependencies:
  coverage: ^1.15.0
  build_runner: ^2.16.1
  drift_dev: ^2.34.4
  http: ^1.2.0
  test: ^1.25.0
  very_good_analysis: ^11.0.0
